# 2026-09-06 工作日志

## M4 几何解码 (M4.1)

### Oracle 采集
- 新增 scripts/m4_geom_oracle.tcl + .py: 一次 hmbatch 会话批量采集多文件 BREP 真值 (points/lines/surfaces/solids)
- 全量采集 65 个含几何文件, 门禁 count=65/65, line2pt + surf2line 拓扑基本全覆盖
- 关键发现 (文档未载):
  - mark id 必须使用 1/2, 高级 mark id (如 9) 静默失败
  - lines <- by surfaces 不支持; 改用反向构建 (surfaces by lines 累加)

### 字节级逆向 - 关键精度发现
- .hm 以 float64 存几何坐标, hm_getvalue 以 float32 精度返回 (相对误差 ~5e-6)
- 此前用 1e-6 绝对容差搜索失败即因此; 必须用相对容差 1e-5

### 几何点段结构 (db 11.05 规整段)
- 段头签名: u32==33056(0x8120) at h, u32(h+12)==256, count=u32(h-8), base=h+20
- 记录 48B/条: (x,y,z) double @+0/+8/+16 + 24B 附加
- 覆盖率: frame_assembly 家族部分覆盖 (段内点); Full_Motion 等碎片化点不覆盖
- 集成到 HMModel.geo_points (parse_geo_points_v3)

### 已知限制 (入册 docs/NYI_INVENTORY)
- 几何存储随编辑历史碎片化 (Full_Motion 步长 52/56/64 混合无规整段头)
- 不同 db 版本段头标记可能不同
- M4.1 仅覆盖 db 11.05 规整段; 碎片化场景需后续逆向或采用「网格重建特征边」过渡方案

## 提交
- e64c1fd feat(M4.1): 几何实体 BREP oracle 采集器
- 1db079a fix(M3.3): others 改 list